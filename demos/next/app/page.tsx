'use client'; import { useTraceGlow } from '@trace-glow/next'; export default function Page() { const telemetry = useTraceGlow(); return <main><h1>Next.js demo</h1><p>Client Web Vitals, performance entries, Fetch, XHR, and server runtime metrics are enabled.</p><button onClick={() => telemetry.logger.info('demo.next.log')}>Capture client log</button><button onClick={() => { const span = telemetry.client.startSpan('demo.next.action'); span.setStatus('ok').end(); }}>Create span</button><button onClick={() => fetch(window.location.href, { cache: 'no-store' })}>Fetch page</button></main>; }
