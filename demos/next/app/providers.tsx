'use client'; import { TraceGlow, TraceGlowProvider } from '@trace-glow/next'; import type { ReactNode } from 'react'; const telemetry = new TraceGlow({ endpoint: process.env.NEXT_PUBLIC_TRACE_GLOW_ENDPOINT ?? 'http://localhost:8080/v1/events', apiKey: process.env.NEXT_PUBLIC_TRACE_GLOW_API_KEY ?? 'demo-browser-write-key', projectId: process.env.NEXT_PUBLIC_TRACE_GLOW_PROJECT_ID ?? 'next-demo', environment: 'development' }); export function Providers({ children }: { children: ReactNode }) { return <TraceGlowProvider telemetry={telemetry}>{children}</TraceGlowProvider>; }
